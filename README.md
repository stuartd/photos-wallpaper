### Photos Wallpaper

In previous versions of macOS you could set it to use your photo library as the wallpaper source, and have it rotate automatically to set a random (or sequential) photo on each monitor at a specified interval.

This functionality is no longer available, so this application is an attempt to recreate it - initially in the simplest way, set a random photo on each desktop at a set interval.
